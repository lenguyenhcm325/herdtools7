# HAND-AUTHORED Layer-1 cram fixture, not generated: the oracle half of
# oracle-negatives.t, paired with obs.txt so one run drives the full 6-cell
# {MATCH,MISMATCH,NO-ORACLE} x {exists,forall} matrix.  SB-sys/MP-sys-F carry
# their real verdicts from tests/gpu-only/expected-nvidia.csv; the `-fa' rows are
# synthetic names supplying the forall half.  The LB rows are ABSENT ON PURPOSE --
# that absence is what produces the NO-ORACLE cells.  Edit with oracle-negatives.t,
# never alone.  ('#' lines and blanks are skipped -- oracle-compare.sh usage block.)
Litmus,Expected,Model,Source
SB-sys,Allowed,PTX,nvidia-ptx.cat
MP-sys-F,Disallowed,PTX,nvidia-ptx.cat
SB-sys-fa,Disallowed,PTX,nvidia-ptx.cat
MP-sys-fa,Disallowed,PTX,nvidia-ptx.cat
