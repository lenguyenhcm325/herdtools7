# HAND-AUTHORED Layer-1 cram fixture, not generated: the oracle half of
# oracle-negatives.t, paired with obs.txt so one run drives the full
# {MATCH,MISMATCH,NO-ORACLE,UNINTERPRETED} x {exists,forall} matrix.  SB-sys/MP-sys-F
# are names the gpu-only corpus really carries; the `-fa' rows are
# synthetic names supplying the forall half.  The LB rows are ABSENT ON PURPOSE --
# that absence is what produces the UNINTERPRETED cells.  WS-sys carries an
# EARNED NO-ORACLE (the oracle has the row and declines to decide it) and
# BOGUS-sys carries a verdict string no harness knows, which must fail closed
# rather than pass.
# Edit with oracle-negatives.t, never alone.  ('#' lines and blanks are skipped -- oracle-compare.sh usage block.)
Litmus,Expected,Model,Source
SB-sys,Allowed,PTX,nvidia-ptx.cat
MP-sys-F,Disallowed,PTX,nvidia-ptx.cat
SB-sys-fa,Disallowed,PTX,nvidia-ptx.cat
MP-sys-fa,Disallowed,PTX,nvidia-ptx.cat
WS-sys,NO-ORACLE,PTX,nvidia-ptx.cat
BOGUS-sys,Perhaps,PTX,hand-corrupted fixture
