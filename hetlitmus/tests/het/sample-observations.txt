# HetLitmus Tier-4 -- SAMPLE observation inputs for the oracle-comparison harness.
#
# These "Observation <name> <verdict> <c_target> <c_other>" lines are the SAME
# shape litmus7 prints after running a test (see litmus/skelUtil.ml:1010,
# "Observation %s %%s %%PCTR %%PCTR").  They are SYNTHESIZED stand-ins, NOT real
# hardware/gem5 measurements: actually running the heterogeneous tests on a
# GH200/MI300A (or in gem5) is Task 9 and is out of Tier-4 scope.  They exist
# only to exercise hetlitmus/oracle-compare.sh end to end.
#
# Lines below are chosen to drive every harness branch against the AMD oracle
# hetlitmus/tests/gpu-only/expected-amd-gcn3.csv:
#   - AMD GPU-only tests that ARE in the oracle  -> MATCH / MISMATCH
#   - heterogeneous GH200 tests (AArch64+PTX)    -> NO-ORACLE (no NVIDIA oracle)
#
# (Comment lines and counts are ignored by the harness; it keys on col 1-3.)

# --- AMD GPU-only corpus: covered by the AMD-GCN3 oracle ---
Observation MP-sys      Sometimes 412 999588      # oracle Allowed     -> MATCH (relaxation seen)
Observation MP-sys-F    Never     0   1000000     # oracle Disallowed  -> MATCH (forbidden, not seen)
Observation IRIW-sys-F  Never     0   1000000     # oracle Disallowed  -> MATCH (forbidden, not seen)
Observation SB-sys-F    Sometimes 3   999997      # oracle Disallowed but SEEN -> MISMATCH (violation)

# --- heterogeneous GH200 tests (AArch64 CPU + PTX GPU): NO AMD oracle ---
Observation MP-het      Sometimes 27  999973      # not in AMD oracle -> NO-ORACLE
Observation SB-het      Never     0   1000000     # not in AMD oracle -> NO-ORACLE
