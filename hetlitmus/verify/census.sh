# The pinned corpus censuses for shell gates -- census.py's mirror, which
# corpus-gate.sh compares the two homes over.  Sourced, never executed.
# The trees the counts are of: paths.sh (HET_CORPUS, X86_CORPUS, GPU_CORPUS).
CENSUS_GPU_ONLY=372
CENSUS_HET=2743
CENSUS_HET_X86=1616
CENSUS_COVER=76
